# cashee
Cashee is a high-performance multithreaded proxy server built in C++ that sits between clients and a backend MySQL database server. It uses an in-memory LRU (Least Recently Used) cache to reduce repeated database queries and improve response time.
